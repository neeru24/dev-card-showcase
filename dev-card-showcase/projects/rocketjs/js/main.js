import { Game } from './Game.js';

// Initialize Game once DOM is ready
window.addEventListener('DOMContentLoaded', () => {
    const game = new Game();
    console.log('ROCKET SIMULATION INITIALIZED');
});
