// Easter Eggs & Fun Modes for GradientFlow
// Hidden modes and playful UI animations

export function activateEasterEgg(mode, engine) {
  switch(mode) {
    case 'disco':
      // ... Rapid color cycling, music, and UI flashes ...
      break;
    case 'matrix':
      // ... Green code rain overlay ...
      break;
    case 'aurora':
      // ... Smooth, flowing aurora gradients ...
      break;
    default:
      // ... Default fun mode ...
      break;
  }
}
